import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderRadius: 4,
    marginRight: 8,
  },
  title: {
    fontWeight: "bold",
    fontSize: 25,
    color: "white",
  },
});
