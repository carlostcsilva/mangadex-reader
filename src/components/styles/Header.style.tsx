import { StyleSheet } from "react-native";

export const HeaderStyles = StyleSheet.create({
  container: {
    backgroundColor: "black",
  },
  title: {
    color: "white",
  },
  backArrow: {
    color: "white"
  }
});
